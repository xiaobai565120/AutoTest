server:
    port: 8888

logging:
    path: logs
    file: mylog.log


spring:
    application:
        name: myTest
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/course?characterEncoding=UTF8&characterSetResults=UTF8&useSSL=false&useUnicode=true&serverTimezone=GMT
        username: autotest
        password: liyu565120

mybatis:
    type-aliases-package: com.course.model
    mapper-locations:
        - mapper/*.xml
